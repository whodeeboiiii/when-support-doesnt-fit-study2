"""화면 문안 (구현명세서 §4 · §9.1).

**문안을 서버에 두는 이유**는 두 가지다.

1. 대조 가능성. §4.6 sidecar 질문과 §7.3 평정 12문항은 [정본]이고 "논문 초안 수록 원문과 글자
   그대로 대조(윤문 금지)"가 §0.4의 동결 항목이다. 문안이 서버 자산이면 자산 계약 테스트가
   명세서 원문과 기계 대조할 수 있다 — 프런트엔드 JSX 안에 흩어져 있으면 사람 눈에 의존한다.
2. 조건 무관성. sidecar 질문은 종결 유형에 따라 두 변형이 있고(§4.6), 그 선택은 서버가 아는
   `user1_disposition`이 결정한다. 클라이언트가 고르게 두면 분기 규칙이 두 곳에 생긴다.

표기(§4 서두): **[정본]** = 글자 그대로 대조 / **[제안]** = PI 승인 대상 / `<TODO>` = 미확정.
[제안] 문안도 명세서 본문에서 그대로 옮긴다 — 승인 전까지는 명세서가 유일한 출처다.

⚠ 참가자 화면에 **조건명·branch 번호·구성 원리**를 넣지 않는다(§4.4·§4.10). 이 파일에 그런
문자열이 생기면 그건 조작 노출이다.
"""

from __future__ import annotations

from dataclasses import dataclass

# --------------------------------------------------------------------------- #
# P0 접속 (§4.0)
# --------------------------------------------------------------------------- #

#: [제안] — 번호가 틀렸는지 코드가 틀렸는지 구분하지 않는다(대입 힌트 방지).
JOIN_FAILED = "번호 또는 접속 코드가 올바르지 않습니다. 연구자에게 문의해주세요."
#: §9.1 접속 코드 만료
CODE_EXPIRED = "연구자에게 문의해주세요."
#: [제안] §2.10·§4.0 데스크톱 가드 (뷰포트 폭 < 1024px) — 클라이언트도 같은 문안을 쓴다.
DESKTOP_ONLY = "이 연구는 데스크톱(노트북) 브라우저에서만 진행할 수 있습니다."
MIN_VIEWPORT_WIDTH = 1024

# --------------------------------------------------------------------------- #
# P1 동의 (§4.1) — 문안 전체가 IRB 착지 대기다
# --------------------------------------------------------------------------- #

#: `<TODO: PH-IRB-1 — 동의서 정본>`. 구조(항목별 복수 체크)만 먼저 세운다.
CONSENT_TODO = "<TODO: PH-IRB-1 — 동의서 정본>"
CONSENT_VERSION = "irb_v0_placeholder"


@dataclass(frozen=True, slots=True)
class ConsentItem:
    """§4.1 필수 포함 항목 ①–⑤. 라벨은 IRB 정본 착지 시 교체한다."""

    field: str
    label: str


CONSENT_ITEMS: tuple[ConsentItem, ...] = (
    ConsentItem("participation", f"① 연구 참여 {CONSENT_TODO}"),
    ConsentItem("study1_data_use", f"② Study 1 자료(사건 기록)의 본 연구 이용 {CONSENT_TODO}"),
    ConsentItem("recording", f"③ 세션 녹화·화면공유·전사(Zoom, 시스템 밖) {CONSENT_TODO}"),
    ConsentItem("overseas_transfer", f"④ 국외 이전(§9.3 — AI2·checker 입력 한정) {CONSENT_TODO}"),
    ConsentItem("withdrawal_and_compensation", f"⑤ 철회 절차·보상(수동 지급) {CONSENT_TODO}"),
)

# --------------------------------------------------------------------------- #
# P3 checkpoint 확인 (§4.3)
# --------------------------------------------------------------------------- #

#: [제안]. **금지**(§4.3): "그때 실제로 무엇을 원했나요?" 류의 선호 재활성화 질문.
CHECKPOINT_INTRO = (
    "지금부터 보시는 것은 Study 1에서 말씀해주신 상황을 대화 형태로 재구성한 것입니다. "
    "당시 상황을 떠올리며 천천히 읽어주세요. 준비가 되면 아래 버튼을 눌러주세요."
)

# --------------------------------------------------------------------------- #
# P4 branch 재진입 (§4.4)
# --------------------------------------------------------------------------- #

#: [제안]. branch 번호·조건명은 표시하지 않는다.
BRANCH_REENTRY = (
    "같은 상황으로 다시 들어갑니다. 이번에도 AI의 답변이 이어집니다. "
    "실제 상황이라면 하실 법한 방식으로 자유롭게 반응해주세요. "
    "답장을 보내지 않거나 대화를 끝내셔도 됩니다."
)

# --------------------------------------------------------------------------- #
# P5 채팅 (§4.5)
# --------------------------------------------------------------------------- #

#: [제안] 버튼 2종. §4.5의 문구 제약 — "답장하지 않아도 됩니다" 류 유도 문구는 두지 않는다.
NO_REPLY_BUTTON = "답장 보내지 않기"
END_BUTTON = "대화 종료하기"
SEND_BUTTON = "보내기"

# --------------------------------------------------------------------------- #
# P6 private sidecar (§4.6)
# --------------------------------------------------------------------------- #

#: [제안]
SIDECAR_TRANSITION = "다음은 AI에게 전달되지 않는 별도 질문입니다."

#: **[정본, 초안 §7.8]** — reply / no_reply·end 두 변형. 윤문 금지.
SIDECAR_QUESTION_REPLY = (
    "방금 답장에 넣지 않았지만, AI의 답변을 보면서 떠올랐던 관련 생각이나 정보가 있었나요?"
)
SIDECAR_QUESTION_NO_REPLY = (
    "방금 AI에게 보내지는 않았지만, 답변을 보면서 떠오른 관련 생각이나 정보가 있었나요?"
)

#: **[정본]** — "있음" 선택 시에만 노출
SIDECAR_HAS_NOTICE = "이 내용은 AI에게 전달되지 않습니다. 가능한 범위에서 적어주세요."
#: **[정본 취지]** 관련성 평정 1–7 (§7.2)
SIDECAR_RELEVANCE_QUESTION = (
    "적어주신 내용이 AI의 다음 응답을 조정하는 데 얼마나 관련 있다고 생각하시나요?"
)
#: [제안] 미전송 이유
SIDECAR_REASON_PROMPT = "이 내용을 방금 AI에게 보내지 않은 이유를 편하게 적어주세요."

#: §4.6 선택지 — 저장값은 §8.1 `sidecar_entries.choice`(none/has/skip)
SIDECAR_CHOICES: tuple[tuple[str, str], ...] = (
    ("none", "없음"),
    ("has", "있음"),
    ("skip", "건너뛰기"),
)
SIDECAR_RELEVANCE_MIN = 1
SIDECAR_RELEVANCE_MAX = 7

# --------------------------------------------------------------------------- #
# P7 AI2 (§4.7 · §9.1)
# --------------------------------------------------------------------------- #

#: [제안]. 참가자 화면에서 정상/재생성/fallback은 **구분되지 않는다**(§4.7).
AI2_LOADING = "AI가 답변을 작성하고 있습니다…"
#: §9.1 AI2 timeout·API 오류
AI2_DELAYED = "답변 준비가 지연되고 있습니다…"

# --------------------------------------------------------------------------- #
# P8 downstream action (§4.8)
# --------------------------------------------------------------------------- #

#: [제안]
DOWNSTREAM_INSTRUCTION = (
    "이 대화를 실제로 계속한다고 가정하면, 지금 무엇을 하시겠어요? 하나를 선택해주세요."
)


@dataclass(frozen=True, slots=True)
class DownstreamOption:
    code: str
    label: str


#: §4.8 — **영문 코드 고정**, 국문 라벨 [제안]. 제시 순서는 명세서 표의 ①–⑦ 그대로다
#: (명세서가 순서 무작위를 지시하지 않는다 — `downstream_actions.display_order`에 이 순서를 남긴다).
DOWNSTREAM_OPTIONS: tuple[DownstreamOption, ...] = (
    DownstreamOption("continue_reply", "이어서 답장한다"),
    DownstreamOption("correct_reformulate", "방금 답변을 수정·재요청한다"),
    DownstreamOption("pause", "잠시 멈춘다"),
    DownstreamOption("end", "대화를 끝낸다"),
    DownstreamOption("new_chat", "새 채팅을 시작한다"),
    DownstreamOption("switch_ai", "다른 AI로 옮긴다"),
    DownstreamOption("seek_human", "사람에게 도움을 구한다"),
)
DOWNSTREAM_CODES: tuple[str, ...] = tuple(option.code for option in DOWNSTREAM_OPTIONS)

# --------------------------------------------------------------------------- #
# P9 평정 (§4.9)
# --------------------------------------------------------------------------- #

#: [제안] 블록 1 — 화면 상단에 해당 branch의 AI1 원문을 회색 카드로 재표시한 뒤 제시한다.
RATINGS_BLOCK1_INSTRUCTION = (
    "다음 두 문항은 방금 대화에서 AI가 보인 첫 답변(위 카드)에 대한 것입니다."
)
#: [제안] 블록 2 — 카드 없음
RATINGS_BLOCK2_INSTRUCTION = "다음 문항들은 방금 경험한 상호작용 전체에 대한 것입니다."
RATINGS_SCALE_MIN_LABEL = "전혀 그렇지 않다"
RATINGS_SCALE_MAX_LABEL = "매우 그렇다"

# --------------------------------------------------------------------------- #
# P10 cross-branch review (§4.10)
# --------------------------------------------------------------------------- #

#: [제안]. sidecar 내용은 참가자 화면에 재표시하지 않는다 `<TODO: PH-02>`.
CROSS_REVIEW_END_BUTTON = "인터뷰가 끝나면 연구자의 안내에 따라 눌러주세요."
#: branch 라벨 — 번호(1–4)로만. 조건명·구성 원리는 표시하지 않는다.
CROSS_REVIEW_BRANCH_LABEL = "{index}번째 대화"

# --------------------------------------------------------------------------- #
# P11 디브리핑 (§4.11)
# --------------------------------------------------------------------------- #

DEBRIEF_TODO = "<TODO: PH-IRB-2 — 디브리핑 정본>"
DEBRIEF_CONFIRM_BUTTON = "확인했습니다"

# --------------------------------------------------------------------------- #
# 오류·복구 (§9.1)
# --------------------------------------------------------------------------- #

SAVE_FAILED = "저장을 완료하지 못했습니다. 다시 시도해주세요."
RESTORING = "진행 중이던 화면을 복구합니다."
#: [제안] 연구자 abort (SS90)
SESSION_ABORTED = "연구자의 안내에 따라 세션이 중단되었습니다."
