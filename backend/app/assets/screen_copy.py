"""화면 문안 (구현명세서 §4 · §9.1).

**문안을 서버에 두는 이유**는 두 가지다.

1. 대조 가능성. **[정본] 7건**은 "논문 초안 신 §7.3·§7.7·§7.8 원문과 글자 그대로 대조
   (윤문 금지)"가 §0.4의 동결 항목이다. 문안이 서버 자산이면 자산 계약 테스트가 명세서
   원문과 기계 대조할 수 있다 — 프런트엔드 JSX 안에 흩어져 있으면 사람 눈에 의존한다.
2. 조건 무관성. 화면 문안이 조건에 따라 달라질 자리를 아예 만들지 않는다.

표기(§4 서두): **[정본]** = 글자 그대로 대조 / **[제안]** = PI 승인 대상 / `<TODO>` = 미확정.
**[PI 승인 <날짜>]** = [제안]이 승인돼 확정된 문안 — 명세서 원문과 같되 대조 테스트는 걸지
않는다([정본]과 달리 §0.4 동결 항목이 아니다).
[제안] 문안도 명세서 본문에서 그대로 옮긴다 — 승인 전까지는 명세서가 유일한 출처다.

⚠ 참가자 화면에 **조건명(C1–C4)·R/U/Q·"focal/대안"이라는 구분 원리·A-level**을 넣지 않는다
(§4 서두). 이 파일에 그런 문자열이 생기면 그건 조작 노출이다. 대안 노출 라벨이 "다른 응답
1/2/3"인 것도(§4.9) 같은 이유다.
"""

from __future__ import annotations

from dataclasses import dataclass

# --------------------------------------------------------------------------- #
# [정본] 7건 — 부록 H.2가 지정한 상수명. **한 글자도 윤문 금지**(CLAUDE.md 절대 규칙 1).
# --------------------------------------------------------------------------- #

#: **[정본, 초안 §7.3]** P2 checkpoint 확인·수정 안내 (§4.2)
CHECKPOINT_VERIFY_INTRO = (
    "다음은 이전에 말씀해주신 실제 AI 사용 경험을 바탕으로 재구성한 내용입니다. "
    "기억하시는 범위에서 사실관계가 명백히 다른 부분이 있으면 알려주세요. "
    "지금 단계에서는 당시 원했던 답이나 속마음을 다시 설명하지 않으셔도 됩니다."
)

#: **[정본, 초안 §7.7]** P4 User1 입력 지시 — 입력창 위 고정 표시 (§4.4)
USER1_INSTRUCTION = (
    "이 연구에서는 대화를 한 번 더 이어간다고 가정합니다. "
    "지금 이 AI에게 실제로 보낼 것처럼 다음 답장을 작성해주세요."
)

#: **[정본, 초안 §7.8]** P5 sidecar 3단 (§4.5 · D-28)
SIDECAR_Q1 = "방금 보낸 답장에는 포함하지 않았지만, 지금 말씀하고 싶은 생각이나 정보가 더 있나요?"
SIDECAR_Q2 = (
    "그 생각이나 정보는 방금 답장을 작성하고 있을 때 이미 떠올라 있었나요, "
    "아니면 지금 이 질문을 보고 새롭게 떠올랐나요?"
)
SIDECAR_Q3 = "답장에 그 내용을 넣지 않은 이유가 있었나요?"

#: **[정본]** P00 자극 3종은 문안이 아니라 **dossier 자산**이다(§5.5) — 여기 두지 않는다.
#: 대조는 `tests/assets/test_p00_canonical_text.py`가 dossier를 상대로 한다.
CANONICAL_COPY: tuple[str, ...] = (
    CHECKPOINT_VERIFY_INTRO,
    USER1_INSTRUCTION,
    SIDECAR_Q1,
    SIDECAR_Q2,
    SIDECAR_Q3,
)

# --------------------------------------------------------------------------- #
# P0 접속 (§4.0 — 승계)
# --------------------------------------------------------------------------- #

#: [제안 승계] — 번호가 틀렸는지 코드가 틀렸는지 구분하지 않는다(대입 힌트 방지).
JOIN_FAILED = "번호 또는 접속 코드가 올바르지 않습니다. 연구자에게 문의해주세요."
#: §9.1 접속 코드 만료
CODE_EXPIRED = "연구자에게 문의해주세요."
#: [제안 승계] §2.10·§4.0 데스크톱 가드 — 클라이언트도 같은 문안을 쓴다.
DESKTOP_ONLY = "이 연구는 데스크톱(노트북) 브라우저에서만 진행할 수 있습니다."
#: §2.10 임계값 (D-38 — 1024 → 768/600). 근거는 `frontend/src/copy.ts`에 함께 적었다:
#: 768은 `.screen` max-width 760px가 포화되는 최소 폭이고, 600은 가로 모드 휴대폰
#: (높이 390–440px)을 걸러내되 데스크톱 창은 건드리지 않는 선이다. 두 값은 클라이언트와
#: 반드시 같아야 한다(NT-19 정적 검사).
MIN_VIEWPORT_WIDTH = 768
MIN_VIEWPORT_HEIGHT = 600

# --------------------------------------------------------------------------- #
# P1 동의 (§4.1) — 초안 문안 착지, IRB 승인 대기
# --------------------------------------------------------------------------- #
#
# 문안 출처는 `docs/IRB_문안_정본_초안_v1.md`(§1-B 라벨 · §1-C 상단 안내 · §1-A ④ 전문)이고
# `tests/assets/test_irb_copy_contract.py`가 그 문서와 글자 대조한다. 명세서 [정본] 5건과
# 같은 규율이되 **대조 대상만 다르다** — IRB 문안의 출처는 명세서가 아니라 IRB 서류다.
#
# ⚠ `CONSENT_TODO`는 **화면에서 내려왔지만 상수는 남는다**. `core/freeze.py`의 모집 게이트가
# 이 문자열에서 `PH-IRB-1`을 찾아 미착지를 판정하기 때문이다(§11.2 · 부록 E.4). 초안은
# 초안이므로 게이트는 계속 ⛔여야 한다 — 승인 전에 이 상수를 지우면 게이트가 잘못 녹색이 된다.
# 승인 후에는 이 상수를 지우고 `CONSENT_VERSION`을 `irb_v1_<승인일>`로 올린다.

#: 모집 게이트 표식 — 참가자 화면에 나가지 않는다(위 주석 참조).
CONSENT_TODO = "<TODO: PH-IRB-1 — 동의서 정본 IRB 승인 대기(초안 문안 착지)>"
#: 초안 단계임을 저장 기록(`sessions.consent_version`·§10.5 `assets_hash`)에 남긴다.
#: 승인 후 정본은 `irb_v1_<승인일>`.
CONSENT_VERSION = "irb_draft_v1_2026-08-24"

#: [초안 §1-C] P1 상단 안내. 서면 동의 후 세션 시작 시의 재확인 화면이다.
CONSENT_NOTICE = (
    "아래 각 항목을 읽고 동의하시면 해당 칸에 표시해 주세요. "
    "본 연구는 모든 항목에 동의하시는 경우에만 참여하실 수 있습니다. "
    "녹화와 데이터 처리 없이는 연구 수행이 불가능하기 때문입니다. "
    "연구 설계의 일부 세부사항은 참여 종료 후 상세히 설명되며, "
    "이 사실을 알고 참여를 결정하시게 됩니다."
)

#: [초안 §1-B 하단] PII 입력 금지 (심의용 연구계획서 19번). P1 하단에 고정 표시한다.
CONSENT_PII_NOTICE = (
    "세션 중 자유롭게 입력하시는 글에는 실명, 기관명, 연락처 등 "
    "본인이나 다른 사람을 알아볼 수 있는 정보를 적지 말아 주세요."
)


@dataclass(frozen=True, slots=True)
class ConsentItem:
    """§4.1 필수 포함 항목 ①–⑥. 라벨은 IRB 정본 승인 시 교체한다."""

    field: str
    label: str


#: §4.1 항목 키 **6종** — ⑥ `alternative_exposure`가 v2 신설이다(대안 노출 고지).
#: 라벨은 초안 §1-B 축약형 그대로(서면 동의서 전문은 §1-A, 시스템 밖).
CONSENT_ITEMS: tuple[ConsentItem, ...] = (
    ConsentItem(
        "participation",
        "① 연구 참여와 절차(약 60분)에 동의했으며, 언제든 중단할 수 있음을 확인했습니다.",
    ),
    ConsentItem(
        "study1_data_use",
        "② 이전 인터뷰의 사건 기록을 재구성해 사용하고, 내가 수정한 내용도 연구 자료가 됨에 "
        "동의했습니다.",
    ),
    ConsentItem(
        "recording",
        "③ 세션 녹화(공유 화면과 음성 한정, 얼굴 미촬영)와 전사에 동의했습니다.",
    ),
    ConsentItem(
        "overseas_transfer",
        "④ AI 응답 생성을 위한 비식별 대화 텍스트의 국외 이전에 동의했습니다.",
    ),
    ConsentItem(
        "withdrawal_and_compensation",
        "⑤ 참여 후 2주 이내 철회 절차와 보상 안내를 확인했습니다.",
    ),
    ConsentItem(
        "alternative_exposure",
        "⑥ 같은 상황에 대한 여러 AI 응답을 보게 된다는 사실을 확인했습니다.",
    ),
)

# --------------------------------------------------------------------------- #
# P2 checkpoint 확인·수정 (§4.2 · D-25)
# --------------------------------------------------------------------------- #

#: [PI 승인 2026-08-24] segment별 수정 UI (PH-13 해소 — 문안·동작 현행 확정).
#: 수정 범위를 segment 일부로 좁히자는 대안은 채택하지 않았다 — segment 단위 유지.
CHECKPOINT_EDIT_BUTTON = "수정"
CHECKPOINT_EDIT_SAVE = "저장"
CHECKPOINT_EDIT_CANCEL = "취소"
#: [제안] 편집창 상단 보조문
CHECKPOINT_EDIT_HINT = "기억하시는 사실과 다른 부분만 고쳐주세요."
#: [제안] 확인 버튼
CHECKPOINT_CONFIRM_BUTTON = "확인했습니다 — 다음으로"
#: §4.2 — 빈 문자열 저장 거부(400)
CHECKPOINT_EDIT_EMPTY = "내용을 비워 둘 수 없습니다."
#: §4.2 — 확인 후 수정 불가(409)
CHECKPOINT_EDIT_CLOSED = "확인을 마친 뒤에는 수정할 수 없습니다."

#: [제안] segment 라벨 — 참가자가 무엇을 고치는지 알아야 한다. 연구 용어는 쓰지 않는다.
SEGMENT_LABELS: dict[str, str] = {
    "situation_summary": "상황 요약",
    "prior_evidence": "대화에서 이미 말한 내용",
    "original_request": "처음 요청",
    "problematic_ai_response": "AI의 답변",
    "trouble_cue": "그 답변에 대해 남긴 말",
}

# --------------------------------------------------------------------------- #
# P3 interactional re-entry 타이머 (§4.3)
# --------------------------------------------------------------------------- #

#: [제안]. §4.3 금지 — 특정 감정·선호·correction 전략을 묻는 문구를 두지 않는다(초안 §7.3).
REENTRY_NOTICE = (
    "잠시 동안, 그때의 상황과 대화 흐름을 기억나는 범위에서 떠올려 주세요. "
    "연구자의 안내가 있으면 아래 버튼을 눌러 진행합니다."
)
#: [제안] 60초 보조문 [파일럿 확정]
REENTRY_READY_NOTICE = "진행하셔도 됩니다"
#: §0.5 [파일럿 확정] — 30초 후 버튼 활성, 60초에 보조문.
REENTRY_MIN_SECONDS = 30
REENTRY_HINT_SECONDS = 60

# --------------------------------------------------------------------------- #
# P4 채팅 — focal AI1 + User1 (§4.4)
# --------------------------------------------------------------------------- #

#: [제안] — **"답장 보내지 않기"·"대화 종료" 버튼 없음**(D-32). 보내기 하나뿐이다.
SEND_BUTTON = "보내기"
#: §4.4 빈 텍스트 전송 거부(400)
USER1_EMPTY = "보낼 내용을 적어주세요"
#: §0.5 [파일럿 확정] — AI1 표시 전 타이핑 인디케이터(focal·대안 동일).
TYPING_INDICATOR_MS = 1500

# --------------------------------------------------------------------------- #
# P5 private sidecar 3단 (§4.5 · D-28)
# --------------------------------------------------------------------------- #

#: [제안 승계]
SIDECAR_TRANSITION = "다음은 AI에게 전달되지 않는 별도 질문입니다."

#: [PI 승인 2026-08-24] 1단 선택지 — 저장값은 `sidecar_entries.has_more`(bool).
#: PH-14 해소 — **「건너뛰기」를 두지 않는다.** 두 선택지뿐이므로 `has_more`는 non-null이고
#: (`tables.SidecarEntry.has_more` nullable=False), 무응답 상태가 존재하지 않는다.
SIDECAR_HAS_MORE_CHOICES: tuple[tuple[bool, str], ...] = ((True, "있어요"), (False, "없어요"))

#: [제안 — 구 초안 정본 승계] 「있어요」 선택 시 자유기술란 고지
SIDECAR_HAS_NOTICE = "이 내용은 AI에게 전달되지 않습니다. 가능한 범위에서 적어주세요."

#: [제안] 2단 선택지 — 저장값은 `sidecar_entries.provenance`.
SIDECAR_PROVENANCE_CHOICES: tuple[tuple[str, str], ...] = (
    ("preexisting", "답장을 쓸 때 이미 떠올라 있었어요"),
    ("prompt_evoked", "지금 이 질문을 보고 떠올랐어요"),
    ("uncertain", "잘 모르겠어요"),
)
#: §7.3 — 3단은 `preexisting`인 경우에만 노출된다(§4.5).
SIDECAR_REASON_PROVENANCE = "preexisting"
#: [제안] 3단은 자유기술 **선택**(빈칸 허용 — §4.5).
SIDECAR_REASON_OPTIONAL_NOTICE = "(선택 — 비워 두셔도 됩니다)"

# --------------------------------------------------------------------------- #
# P6 AI2 (§4.6 · §9.1)
# --------------------------------------------------------------------------- #

#: [제안 승계]. 참가자 화면에서 정상/재생성/fallback은 **구분되지 않는다**(§4.6).
AI2_LOADING = "AI가 답변을 작성하고 있습니다…"
#: §9.1 AI2 timeout·API 오류
AI2_DELAYED = "답변 준비가 지연되고 있습니다…"

# --------------------------------------------------------------------------- #
# P7 User2 / 종료 (§4.7 · D-26)
# --------------------------------------------------------------------------- #

#: [제안]
DOWNSTREAM_INSTRUCTION = "AI의 답변을 보셨습니다. 실제 상황이라면 지금 어떻게 하시겠어요?"
#: [제안] 두 갈래 라벨 — 좌우 고정(§4.7).
DOWNSTREAM_REPLY_LABEL = "답장 이어쓰기"
DOWNSTREAM_END_LABEL = "여기서 대화를 마치기"
#: [제안] User2 전송 후 안내. **AI 응답 없음**(D-33 — AI3는 없다).
USER2_SENT_NOTICE = "이 연구에서는 대화를 여기까지만 이어갑니다. 다음으로 진행해주세요."
#: [제안 지시문] 이탈 이유 — 필수 1자 이상 `[파일럿 확정: 필수 여부]`
END_REASON_PROMPT = "이렇게 선택한 이유를 편하게 적어주세요."
END_REASON_REQUIRED = True


@dataclass(frozen=True, slots=True)
class EndTypeOption:
    code: str
    label: str


#: §4.7 — **영문 코드 고정**, 국문 라벨 [PI 승인 2026-08-24] (PH-09 해소 — 6코드·라벨·순서 현행 확정).
#: 이유 필수 여부(`END_REASON_REQUIRED`)만 `[파일럿 확정]` 창에 남는다(§8 표).
#: 제시 순서는 표 순서 고정(무작위 아님 — `display_order`에 이 순서를 남긴다).
#:
#: `no_further_need`와 `stop_here`를 구분해 두는 이유는 Study 1 5.4(대화 중단 ≠ 지원 불필요)
#: 이며, **이 구분을 시스템이 해석하지 않는다**(§4.7).
END_TYPE_OPTIONS: tuple[EndTypeOption, ...] = (
    EndTypeOption("stop_here", "이 대화를 여기서 멈춘다"),
    EndTypeOption("new_chat", "같은 AI에서 새 대화를 시작한다"),
    EndTypeOption("switch_ai", "다른 AI에게 물어본다"),
    EndTypeOption("seek_human", "사람에게 이야기한다"),
    EndTypeOption("no_further_need", "더 이상 도움이 필요하지 않다"),
    EndTypeOption("other", "기타 (직접 입력)"),
)
END_TYPE_CODES: tuple[str, ...] = tuple(option.code for option in END_TYPE_OPTIONS)
#: §4.7 — `other`는 직접 입력이므로 이유가 반드시 필요하다(NT-41).
END_TYPE_REQUIRES_TEXT = "other"

# --------------------------------------------------------------------------- #
# P8 focal measures + manipulation check (§4.8)
# --------------------------------------------------------------------------- #

#: §0.5 — 전 문항 1–7.
RATINGS_SCALE_MIN_LABEL = "전혀 그렇지 않다"
RATINGS_SCALE_MAX_LABEL = "매우 그렇다"
#: 블록별 지시문은 **자산**에 있다(`fixtures/focal_items_v1.json`) — 문항과 함께 승인된다.

# --------------------------------------------------------------------------- #
# P9 대안 노출 ×3 (§4.9 · D-29)
# --------------------------------------------------------------------------- #

#: [PI 승인 2026-08-24] 첫 대안 진입 시 1회 안내 (PH-10 해소 — 명세 §4.9 [제안] 문안 그대로).
ALT_EXPOSURE_INTRO = (
    "지금부터는 같은 시점에서 AI가 다르게 응답했다면 어땠을지를 보여드립니다. "
    "서로 다른 세 가지 응답을 차례로 보여드리겠습니다. "
    "이번에는 답장을 작성하지 않으셔도 됩니다."
)
#: [제안] 화면 상단 라벨 — **조건명 없음**(§4.9).
ALT_EXPOSURE_LABEL = "다른 응답 {position}"
#: [제안] 진행 버튼. 3번째는 비교 단계로 넘어간다.
ALT_NEXT_BUTTON = "다음 응답 보기"
ALT_LAST_BUTTON = "비교로 넘어가기"

# --------------------------------------------------------------------------- #
# P10 pairwise ×3 (§4.10)
# --------------------------------------------------------------------------- #

#: [PI 승인 2026-08-24] (PH-10 해소 — 명세 §4.10 [제안] 문안 그대로).
PAIRWISE_INTRO = (
    "같은 시점에서의 두 응답을 비교합니다. 두 응답을 읽고 아래 문항에 답해주세요. "
    "이어서 연구자가 몇 가지 질문을 드립니다."
)
#: §4.10 — 두 열의 라벨. **어느 쪽이 focal이었는지 라벨링하지 않는다**.
PAIRWISE_SIDE_LABELS: tuple[str, str] = ("응답 A", "응답 B")
#: [제안] 상단 effective checkpoint 접이식 카드(기본 접힘)
PAIRWISE_CHECKPOINT_TOGGLE = "상황 다시 보기"

# --------------------------------------------------------------------------- #
# P11 contrastive interview 대기 (§4.11)
# --------------------------------------------------------------------------- #

#: [제안 승계]. 문항·응답값은 재표시하지 않는다(§4.11).
INTERVIEW_HOLD_BUTTON = "인터뷰가 끝나면 연구자의 안내에 따라 눌러주세요."
#: [제안] pair 라벨 — 제시된 순서 그대로. 조건명·contrast 명칭은 쓰지 않는다.
INTERVIEW_PAIR_LABEL = "{position}번째 비교"

# --------------------------------------------------------------------------- #
# P12 디브리핑 (§4.12)
# --------------------------------------------------------------------------- #

# 문안 출처는 `docs/IRB_문안_정본_초안_v1.md` §2-A이고 계약 테스트가 그 문서와 대조한다.
# `DEBRIEF_TODO`는 CONSENT_TODO와 같은 이유로 남는다 — 모집 게이트의 미착지 표식이다.
# 대괄호 슬롯 3종(`[IRB 승인번호]`·`[연구팀 연락처]`·`[IRB 사무국 연락처]`)은 승인 후 치환한다.

#: 모집 게이트 표식 — 참가자 화면에 나가지 않는다.
DEBRIEF_TODO = "<TODO: PH-IRB-2 — 디브리핑 정본 IRB 승인 대기(초안 문안 착지)>"

#: [초안 §2-A] P12 본문. §4.12 필수 공개 ①–⑦을 순서대로 담는다.
DEBRIEF_BODY = """참여해 주셔서 감사합니다. 이제 이 연구의 설계에 대해 참여 시점에 말씀드리지 않았던 부분을 설명드립니다.

(①) 오늘 보신 네 개의 AI 응답은 같은 사건의 같은 시점에 대해 AI가 보일 수 있는 서로 다른 대응 방식이었습니다. 이 연구는 사용자가 문제를 표시한 직후 AI가 어떻게 반응하는 것이 좋은지를 여러 방식 간 비교를 통해 살펴보고 있으며, 이를 미리 상세히 알려드리면 답장과 평가가 달라질 수 있어 종료 후에 설명드리게 되었습니다.

(②) 처음에 직접 답장을 작성하셨던 응답과 나중에 보신 세 응답 중 어느 것도 "정답"이 아닙니다. 네 응답 중 무엇을 먼저 경험하실지는 무작위로 정해졌으며, 참여자님의 어떤 특성에 대한 판단도 아닙니다.

(③) 처음 제시된 AI 응답들은 참여자님이 이전 인터뷰에서 말씀해 주신 실제 사건을 바탕으로 연구진이 사전에 작성하고 검증하여 고정해 둔 것이었습니다.

(④) 두 번째 AI 응답은 언어모델이 그 자리에서 생성했으며, 모든 참여자에게 동일한 사전 승인 정책과 자동 검증이 적용되었습니다.

(⑤) 답장 뒤에 여쭤본 별도 질문(답장에 담지 않은 생각)에 적어주신 내용은 AI에게 전달되지 않았고, 앞으로도 전달되지 않습니다. 연구자만 열람하는 분석 자료로 보관됩니다.

(⑥) 대화 재구성 화면에서 수정해 주신 내용은 수정 반영본으로 연구 자료에 포함되었으며, 두 번째 AI 응답을 만들 때 입력으로 사용되었습니다.

(⑦) 참여 후 2주 이내에 연구팀에 요청하시면 이유를 밝히지 않고 자료를 철회하실 수 있으며, 철회는 보상과 무관합니다. 보상은 세션 종료 후 남겨주신 휴대전화번호로 연구자가 직접 발송합니다. 궁금한 점이나 불편한 점은 언제든 아래 연락처로 문의해 주세요.

연구과제명: 생성형 AI와의 일상 고민 대화에서 지원 불일치 경험과 AI의 후속 반응 방식에 관한 연구
IRB 승인번호: [IRB 승인번호]
연구팀 연락처: [연구팀 연락처]
기관생명윤리위원회: [IRB 사무국 연락처]"""

DEBRIEF_CONFIRM_BUTTON = "확인했습니다"

# --------------------------------------------------------------------------- #
# 오류·복구 (§9.1)
# --------------------------------------------------------------------------- #

SAVE_FAILED = "저장을 완료하지 못했습니다. 다시 시도해주세요."
RESTORING = "진행 중이던 화면을 복구합니다."
#: [제안] 연구자 abort (SS90)
SESSION_ABORTED = "연구자의 안내에 따라 세션이 중단되었습니다."
