/** @type {import('tailwindcss').Config} */

/*
 * 참가자 UI 디자인 토큰 (§2.10 · D-39).
 *
 * ── 색의 뜻 ────────────────────────────────────────────────────────────────
 * 배경은 흰색이다. 그 위에 **세 층**만 얹는다.
 *
 * 1. **파랑(accent) = 시스템이 참가자에게 요구하는 것.** 1차 버튼(채움), 포커스 링,
 *    선택 상태(테두리+연한 배경), 그리고 재구성된 사건 카드(연파랑 배경).
 *    - 채움 vs 테두리로 "눌러야 할 것"과 "내가 고른 것"을 가른다. 구 주석은 선택을 검정으로
 *      두어 갈랐지만, 검정 채움은 참가자 말풍선(ink)과 같은 색이라 화면에 검정 덩어리가
 *      세 종류(말풍선·선택칩·비활성) 생겼다. **채움/윤곽** 구분이 더 안정적이다.
 * 2. **노랑(guide) = 연구자가 참가자에게 건네는 말.** 화면 상단 지시문 블록 전용이다.
 *    버튼에는 쓰지 않는다 — 노랑 위 흰 글자는 대비가 안 나온다.
 * 3. **무채색 = 자극.** AI 말풍선은 흰 배경 + 실선 테두리다. 색조를 **넣지 않는다**:
 *    AI 버블의 색조는 따뜻함 지각을 건드리고, 그건 recognition·uptake 평정(초안 §7.11)과
 *    교락한다. 이 규칙은 `test_ai_bubbles_have_no_color_tint`가 지킨다.
 *
 * 대비 실측(WCAG, 흰 배경): accent.deep 7.56:1 · accent 5.93:1 · accent.soft 1.15:1.
 * accent.soft·guide.soft는 **배경 전용**이다. guide 위 본문은 ink(#111827)로 얹는다.
 *
 * ── 새 응답 하이라이트 ─────────────────────────────────────────────────────
 * focal AI1 · 대안 AI1 3종 · AI2는 **완전히 같은** 클래스(`.bubble-new`)로 표시한다.
 * 애니메이션·keyframes는 여기가 아니라 `index.css`에 있다 — 이유는 그 파일 주석 참조.
 * 조건마다 표시가 달라지면 그 자체가 조작이 된다. 한 곳에서만 정의하는 이유다.
 *
 * 다크모드는 도입하지 않는다 — 자극의 표시 조건이 참가자 기기 설정에 따라 갈리면 자극
 * 동일성이 깨진다(§0.4 조작 안정성).
 *
 * ⚠ 데스크톱 전용이다(D-12·§2.10). 모바일 대응 CSS·터치 타깃 규칙은 작성하지 않는다.
 */

export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        ink: '#111827',
        edge: '#9AA5B1',
        hair: '#E3E8EE',
        accent: {
          DEFAULT: '#0369A1',
          deep: '#075985',
          soft: '#E0F2FE',
        },
        /*
         * 자극(AI 말풍선) 전용 무채색. **R·G·B가 모두 같은 값이다** — 색상(hue)이 0이므로
         * 따뜻함/차가움 지각을 건드리지 않는다. 제약은 색조이지 명도가 아니라서, 흰색이
         * 아니라 회색으로 채워도 된다(오히려 흰 카드 위에서 말풍선이 식별된다).
         * `test_ai_bubble_fill_is_achromatic`이 R=G=B를 검사한다.
         */
        stim: {
          fill: '#F2F2F2',
          line: '#E4E4E4',
          //: AI1의 무대지시(D-40) 전용. 자극 본문(ink)보다 흐리되 fill 위에서 4.6:1로
          //  읽힌다. 여기도 R=G=B다 — 무대지시에 색조를 넣으면 그 문장만 다른 톤이 된다.
          note: '#6E6E6E',
        },
        //: 보조색 — 지시문 블록 전용. `line`은 좌측 굵은 선, `soft`는 배경, `ink`는 강조 텍스트.
        guide: {
          DEFAULT: '#F59E0B',
          line: '#D97706',
          soft: '#FEF7E6',
          ink: '#78350F',
        },
      },
    },
  },
  plugins: [],
}
